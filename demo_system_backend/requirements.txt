# demo_system_backend — pip requirements
#
# NOTE: torch, torchvision, and Python itself are NOT listed here.
# Install them first via conda (see README.md for exact versions):
#   conda create -n demo_backend python=3.9.13
#   conda activate demo_backend
#   pip install torch==2.1.0 torchvision==0.16.0 --index-url https://download.pytorch.org/whl/cu121

# ── YOLO pose detection + BoT-SORT tracking ──
ultralytics==8.3.141
lap==0.5.12

# ── Computer vision ──
opencv-python==4.11.0.86
numpy==1.24.4

# ── PCA (biomarker) ──
scikit-learn==1.6.1

# ── Utilities ──
tqdm==4.65.2
